# Codesphere OpenBao Template

**Note**: 
This is not production ready. A real vault setup is not fully automatable. For dev it is sufficient. 

### dev setup
just start openbao, but it is only for dev setup!

### prod setup
not fully production ready.but if you want to try now, then do the following:
1. start run stage
2. go to a terminal and execute `./scripts/unseal.sh`
3. now you can use openbao